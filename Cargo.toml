[package]
name = "pair"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
nanoserde = { version = "0.2.1", default-features = false, features = ["std", "json"] }
ureq = "3.1.4"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
