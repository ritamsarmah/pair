[package]
name = "pair"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
reqwest = { version = "0.12.22", features = ["blocking", "json"] }
serde_json = "1.0.140"

[profile.release]
lto = true
panic = "abort"
strip = true
